{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compileMaven",
            "type": "shell",
            "command": "mvn",
            "args": ["compile", "-q"],
            "options": {
                "cwd": "${workspaceFolder}/LockerEasy"
            },
            "problemMatcher": []
        }
    ]
}
